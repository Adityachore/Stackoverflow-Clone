# MongoDB Connection String
# Replace with your actual MongoDB connection string
# For local MongoDB: mongodb://localhost:27017/stackoverflow-clone
# For MongoDB Atlas: mongodb+srv://username:password@cluster.mongodb.net/stackoverflow-clone
MONGODB_URI=mongodb://localhost:27017/stackoverflow-clone

# CORS Allowed Origins (comma-separated for multiple origins)
# For development: http://localhost:3000
# For production: https://stack-clone-elevance.netlify.app
CORS_ORIGIN=http://localhost:3000, https://stack-clone-elevance.netlify.app

# Server Port
PORT=5000

# JWT Secret Key
# Generate a secure random string for production
# You can use: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your_super_secret_jwt_key_change_this_in_production

# JWT Expiration Time
JWT_EXPIRE=7d

# Node Environment
NODE_ENV=development
